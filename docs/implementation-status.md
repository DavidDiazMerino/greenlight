# Implementation status

Last updated: 2026-08-23.

## VERIFIED LOCAL

- Dependency-free Node/TypeScript runner and static Decision Card build.
- Eight original synthetic 16:9 cases with locked captions and edit plans.
- Actual PNG and H.264 MP4 generation through the installed FFmpeg.
- Baseline and release-candidate caption compositors with a reproducible multiline coordinate-space defect.
- Pixel-diff caption bounds measured from decoded RGB frames against a no-caption render.
- FFprobe output dimensions/duration validity, run coverage, structured logs, trace-shaped evidence, Prometheus metrics, evidence hashing, and deterministic policy evaluation.
- Local/synthetic provenance and MCP-required insufficient-evidence guardrail.
- Typed `Change`, provenance-preserving `EvidenceItem`, `Signal`, `EvidenceResilienceAssessment`, `EvidenceCasefile`, versioned `CanaryPack`/`Invariant`, `CanaryRun`, immutable `DecisionReceipt`, and fixture-level `DecisionOutcome` records.
- Explicit `evidence-confidence/v1` components and content fingerprints; this is a deterministic heuristic and is not statistically calibrated.
- Applicability, paired baseline-pass/candidate-fail reproduction, full pack coverage, authoritative-within-fixture source counts, a disclosed non-blocking timing contradiction, recommendation eligibility, and suppression reasons.
- A versioned eight-case Canary Pack with four named invariants. The caption safe-area invariant blocks rc1; policy ignores any AI-style suggested action.
- Content-addressed decision receipts and an outcome fixture explicitly marked `fixture/not-observed`, with no production outcome claim.
- Expanded accessible Decision Card evidence details, workflow relevance, confidence basis, contradiction, reproduction, named invariants, deterministic policy owner, and receipt fingerprint while retaining the media comparison.

Exact latest measurements and verification commands are generated by `make canary` and documented in the engineering handoff; do not copy example performance numbers into prose.

## IMPLEMENTED BUT NOT LIVE-VERIFIED

- Grafana MCP adapter boundary with runtime tool discovery, exact-call receipts, and strict evidence validation.
- OTLP/HTTP exporter boundary for a real OpenTelemetry receiver.
- Gemini/Google ADK experiment-planner boundary with output and immutable-policy validation.
- Local web server and static build suitable for a later private or public deployment.

No cloud service, MCP server, Gemini model, ADK runtime, or hosted Grafana instance was contacted. `authoritative: true` in local casefiles means authoritative for this repository-owned fixture only; it does not mean independent upstream confirmation.

## NOT IMPLEMENTED

- Google Cloud Run Jobs/web deployment, Firestore/GCS persistence, cloud IAM, or public URL.
- Live Grafana metrics/Loki/Tempo ingestion and official MCP queries.
- Live Gemini/Google ADK agents, including the Evidence & Decision Agent.
- Changelog polling, generated creative media during canaries, social publishing, alerts, multi-pipeline support, or subjective LLM QA.
- Edited three-minute submission video.
- Observed production outcomes, historical confidence calibration, source reliability scoring, or third-party change collection.
