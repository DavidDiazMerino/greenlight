{
  "name": "greenlight-agentic-cinema",
  "version": "0.1.0",
  "private": true,
  "description": "A deterministic, evidence-backed release gate for vertical-video post-production.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "install:local": "npm install --ignore-scripts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test --test-isolation=none test/*.test.ts",
    "canary": "node --experimental-strip-types src/cli.ts canary",
    "grafana:setup": "node --env-file=.env --experimental-strip-types src/cli.ts grafana-setup",
    "agent:live": "node --env-file=.env --experimental-strip-types src/cli.ts agent-live",
    "live:sanitize": "node --experimental-strip-types scripts/sanitize-live-verification.ts",
    "demo-fixture": "node --experimental-strip-types src/cli.ts demo-fixture",
    "dev": "node --experimental-strip-types src/server.ts",
    "build": "node --experimental-strip-types scripts/build.ts",
    "public:live": "node --experimental-strip-types scripts/apply-public-live-verification.ts",
    "receipt:sign": "node --env-file-if-exists=.env --experimental-strip-types scripts/sign-decision-receipt.ts",
    "receipt:verify": "node --experimental-strip-types scripts/verify-decision-receipt-signature.ts"
  },
  "dependencies": {
    "@google-cloud/kms": "^5.7.0",
    "@google/adk": "^2.0.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "overrides": {
    "adm-zip": "^0.6.0",
    "qs": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
